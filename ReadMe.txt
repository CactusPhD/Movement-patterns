Ryan Baird

So for all simulations a click moves the goal and 'esc' goes back to the menu.
For interpose, wander, and flocking you can toggle the goal being moved with 't'.
In all except flocking 'r' will reset the AI to its starting position.